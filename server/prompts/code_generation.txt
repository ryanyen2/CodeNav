## AddNode
Generate exactly one Python function that implements the given semantic feature.
Rules:
- Output only one function definition and its body. Do not output multiple functions or duplicate definitions.
- No explanation, no markdown, no code fence.
- Use a clear docstring that reflects the feature.
- Preserve the requested signature if given.
- Do not generate helper functions or imports; aim for 15 lines or fewer.

## EditFeature (small function, full rewrite)
Modify the given Python function so its behavior matches the new semantic feature.
Keep the same function name and preserve the signature unless the feature requires a change. Keep it concise.
Output only the modified function (no explanation, no markdown).

## EditFeature (large function, minimal edit)
Make MINIMAL changes only. Use exactly one SEARCH/REPLACE block:
<<<<<<< SEARCH
<exact lines to replace>
=======
<new lines>
>>>>>>> REPLACE
Do not rewrite the entire function.

NO explanation, no markdown, no code fence, no docstring and no comments in the code.
