// The action vocabulary, re-exported.
//
// The file itself lives with the logger, because that is what ships inside the
// extension a participant installs and it has to be self-contained there. There
// is one copy, and this is the door into it from the web apps.
export * from '../../docs/study-materials/logger/actions-vocab.js';
