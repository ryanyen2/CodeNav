You are applying code changes to align a codebase with edits made to its **codoc
feature tree** (a tree of human intent that maps to code). Working directory:
{root_dir}

Apply the MINIMUM code changes needed to satisfy each item below. Do not invent
features beyond what is stated. Preserve existing tests unless they directly
conflict with the new intent. Prefer small, surgical edits over large rewrites.

## Constraints (hard)

- Edit ONLY the files named in each item's `Edit only:` line. If an item names
  no files, create the smallest new file/symbol that fits — do not refactor
  unrelated code to host it.
- NEVER edit or delete anything under `.codoc/` — that is codoc's own state,
  not your target. In particular do not touch `.codoc/tree.codoc` (codoc
  maintains the feature descriptions itself) and do not delete
  `.codoc/realize.md` or `.codoc/realize.json` — the queue closes itself as
  your cited reflections land, and deleting it destroys any item the user
  appended while you worked.
- Do not rename, reword, or "tidy" features, descriptions, or symbols that no
  item asked you to change.

## Reading the items

- `STEER FEATURE` items carry an `Author note:` — an inline comment the user
  addressed to you mid-flight. Apply the note to that feature's code; where the
  note conflicts with the feature's description, the note wins.
- A `Focus:` line lists phrases the author **bolded** in the doc. Treat them as
  the highest-priority part of that item's intent — get those exactly right
  before anything else.
- A `Consult:` line cites an external page. Fetch and read it (WebFetch) before
  implementing that item — it carries details the intent text omits.
- An `Author asked:` line quotes what the human actually requested this session,
  in their own words. It is the ground-truth goal — implement THAT; where the
  reconstructed intent text and the author's ask differ, the author's ask wins.

{{doclang}}

## Changes to apply

{directives}

Each item's heading carries its directive id (`⟨d-…⟩`). When you reflect an
item's code via `codoc_reflect`, pass that id as `caused_by` so the change is
attributed to the tree edit that requested it — that citation is ALSO what
marks the item done; the queue files remove themselves once every item has
evidence. Re-read `.codoc/realize.md` after each item: the queue can grow
while you work.

When you are done, the code should faithfully reflect each item's intent — and
nothing outside the listed scope should have changed.
