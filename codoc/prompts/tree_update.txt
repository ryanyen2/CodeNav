You maintain the **feature tree** for the codebase "{repo_name}". A feature tree
is a small, navigable hierarchy of *intent*: each node is a named unit of what
the code is for, and binds to one or more code chunks (a chunk = a function,
method, class, or module identified by `file::symbol_path`).

The code just changed. Your job: return the MINIMAL set of operations that keeps
the tree faithful to the code, WITHOUT creating duplicate or near-duplicate
nodes.

## What changed (the change set)

{changes}

- `added`   — chunks that appeared and are not yet bound to any feature.
- `removed` — chunks that disappeared; `current_feature_id` is the feature they
  were bound to. A feature listed here has just lost its last binding.
- `modified` — chunks whose code changed but stay bound to `current_feature_id`;
  use these only to decide whether that feature's description is now stale.

## The relevant subtree (full detail)

These are the existing features near the change (same files + their parents and
children). Prefer attaching new chunks here.

{subtree}

## Every feature title in the tree (for de-duplication)

{all_titles}

## Operations you may emit

- `attach`      — bind added chunk(s) to an EXISTING feature. Fields: `feature_id`, `bindings`.
- `add_node`    — create a NEW feature for chunk(s) no existing node covers.
                  Fields: `parent_id` (omit/null = top level), `title`, `description`, `bindings`.
- `amend`       — update a feature's `title` and/or `description` (e.g. modified code
                  shifted its meaning). Fields: `feature_id`, `title?`, `description?`.
- `move_node`   — reparent a feature (uplift / restructure). Fields: `feature_id`, `parent_id`.
- `retire_node` — retire a feature that lost all its code. Fields: `feature_id`.

Every op may carry a one-line `rationale`.

## Rules

1. Return the FEWEST ops that capture the change. Doing nothing is valid — emit
   `"ops": []` if the change is cosmetic and no node needs updating.
2. Strongly prefer `attach` to an existing feature over `add_node`. Only add a
   node when no feature in the subtree plausibly covers the new chunk.
3. NEVER create a node whose `title` duplicates or paraphrases any title in the
   tree (see the full title list above). If two added chunks belong together,
   cover them with ONE node, not two.
4. Choose `parent_id` from the subtree/titles so the new node sits under the most
   related parent. Keep the tree shallow and balanced (aim for 3–7 children per
   parent); top-level nodes are broad themes.
5. Only `amend` a description when modified code genuinely changed what the
   feature does — not for every edit.
6. A `title` is a 3–6 word sentence-case noun phrase ("Index snapshot diff").
   A `description` is 1–3 sentences: what the feature does and why it exists. No
   code-location lists in the prose — bindings carry that.

## Output

Return ONLY this, inside solution tags:

<solution>
{"ops": [
  {"kind": "attach", "feature_id": "f-1a2b", "bindings": [["codoc/loop/diff.py", "codoc/loop/diff.py::compute_changeset"]], "rationale": "same snapshot-diff behavior as the existing node"},
  {"kind": "add_node", "parent_id": "f-09cd", "title": "Relevant subtree selection", "description": "Picks the minimal set of features the tree-update LLM needs, using binding file-locality.", "bindings": [["codoc/loop/subtree.py", "codoc/loop/subtree.py::select_relevant_subtree"]], "rationale": "no existing node covers subtree scoping"}
]}
</solution>
