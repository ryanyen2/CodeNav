You are building the initial **feature tree** for the codebase "{repo_name}" — a
navigable hierarchy of *intent*, where each node is a named unit of what the code
is for and binds to code chunks (a chunk = a function, method, class, or
module-level block, identified by `file::symbol_path`).

You are looking at ONE file at a time. Given the file's code chunks and the
call/containment relationships among them, produce a SMALL, coherent set of
features that captures what that file is for.

## How to choose nodes

1. **Group coarsely — prefer FEW nodes.** A small or single-purpose file is
   usually ONE feature. Only split a file when it clearly does two or more
   unrelated things.
   - **Upper-bound check:** If a single node would own more than ~12 bindings,
     ask whether the bindings represent genuinely one concern. If your description
     needs to comma-list 4+ distinct subsystems (e.g. "config, attention, MLP,
     generation, analysis"), split it — that description is signaling multiple nodes.
   - **Two parallel implementations rule:** If a file contains two classes with
     independent implementations of the same interface (e.g. two different tokenizer
     backends, two different optimizer variants), give each class its own node rather
     than lumping them. They serve different purposes even if they share a protocol.
2. A class and its methods are normally ONE feature (the class). Only break out a
   cluster of methods as a CHILD node when they form a clearly separable concern.
3. **Every chunk listed MUST be bound by exactly one node you emit.** Bind
   module-level chunks (`<file>::__module__`) to the file's primary feature, not a
   node of their own. Bind module-level constants to the node that primarily *uses*
   them, not to a generic utilities node.
4. Do not invent a node whose title merely paraphrases one already created from
   another file (see the list below); name this file's intent on its own terms.

## Relationships legend

- `calls` / `called_by` are other symbols (some in other files) — use them to
  judge cohesion, NOT to bind anything outside this file.
- `contained_in` means the symbol is a method/member of that class.

## Nesting (within this file)

To nest, give each new node a temporary id (`"id": "n1"`) and set a child's
`"parent_id"` to that temporary id. Parents must appear before their children.

## Field rules

- `title`: a 3–6 word sentence-case noun phrase naming the intent ("HTTP
  transport adapter") — NEVER the bare filename.
- `description`: ONE paragraph of 1 to 3 sentences that hands a newcomer the
  *theory* of the feature — what they need to know to change this code
  confidently. Open with its purpose: the problem it solves, in plain everyday
  words a reader who has never seen the code understands. Then, when you can
  say it, why it is built this way — the constraint, failure, or tradeoff the
  design answers — and what would break if someone changed it without knowing.
  Write it as flowing prose, not as labelled parts.

  **These are the tree's leaves, so write at leaf altitude** (see "Write at the
  altitude of the node you are on" below). A later pass groups them under broad
  themes and writes those; nothing deeper than what you write here will ever be
  added. So this is the place for the actual symbol, the actual constant, and the
  actual number — a description that stays so general it could be about another
  codebase has left the reader nowhere else to go. The one exception is a node you
  give a child of its own: that parent is read by somebody choosing between its
  children, and it should hold without a symbol in it.

  **Say the why as plainly as your evidence allows, and no further.** You have
  three sources of intent and all three count:

  1. **The brief below**, which is what the project says about itself.
  2. **Comments and docstrings in the code you are shown.** These are the author
     writing down what the code cannot say, and they are evidence in exactly the
     way a commit message is. A comment that says a hyphen is dropped because the
     typesetter put it there IS the reason; report it. A comment naming what a
     choice gives up, or saying that one step must run before another, is the
     most valuable thing in the file — those are the things a reader cannot
     recover from the code and will otherwise break.
  3. **The commit history**, when it states a reason.

  When any of them states a reason, assert it flatly — e.g. retries only on
  timeout, because the server can duplicate a non-idempotent post. Where none
  does, describe what the code achieves rather than narrating a decision — e.g.
  computes the total on write, so a reader never sees a stale number, which
  needs no warrant.

  **Where the code chose between two defensible options, say which and why.**
  Most rules in most codebases could have gone the other way, and which way this
  one went is the single thing a reader most needs and can least infer. If the
  source says what the alternative was, or what this choice costs, that belongs
  in the description ahead of anything about how it is implemented. Write descriptions as plain prose with NO double-quote
  characters in them; every field here is a JSON string, and a bare `"` inside
  one makes the whole response unparseable.
  NEVER invent history, people, or rejected alternatives — no "added after users
  reported", no "the team chose X over Y". If you were not shown a reason, do
  not report one. Do not hedge with "presumably" / "appears to" either; state
  behavior flatly and reasons only where you have them.

  No jargon, no restating the title, no lists of code locations (bindings carry
  that). You MAY include AT MOST one inline citation
  `[symbol()](codoc:file.py#symbol)` — file and symbol verbatim from your own
  bindings — when one symbol anchors the claim. Never emit lines starting with
  `> ` or `**bold**` spans (reserved author channels). NEVER leave it empty.
- `bindings`: the `[file, symbol_path]` pairs this node owns.

## Output — return ONLY this, inside solution tags (example for a file `adapters.py`):

<solution>
{"ops": [
  {"id": "n1", "kind": "add_node", "title": "HTTP transport adapter", "description": "Carries a prepared request onto the network, handing it to urllib3 and managing the connection pools, proxies, and TLS so that a caller never touches a raw socket.", "bindings": [["adapters.py", "adapters.py::HTTPAdapter"], ["adapters.py", "adapters.py::HTTPAdapter.send"]], "rationale": "the file's primary class"},
  {"id": "n2", "kind": "add_node", "parent_id": "n1", "title": "Connection pool configuration", "description": "Reuses connections instead of opening one per request, by keeping a pool for each host and proxy pair, so a second request to the same host skips the TCP and TLS setup entirely.", "bindings": [["adapters.py", "adapters.py::HTTPAdapter.get_connection"]], "rationale": "separable pooling concern"}
]}
</solution>

{{include:style}}

{{doclang}}
<<<CACHE_BREAK>>>
## The brief — what this project is, read from its own prose

Formed before any file was looked at, from the README and the opening of every
source file. Use it to place this file inside the whole, to use the project's own
vocabulary, and to know which of its rules were choices. Where it and the code in
front of you disagree, the code is what is true now; say what the code does.

{brief}
<<<CACHE_BREAK>>>
## Features already created from other files (for naming consistency only — do NOT bind to them)

{existing_titles}
<<<CACHE_BREAK>>>
## The file to analyze: `{file}`

### Chunks in {file}

{chunks}

### Relationships near these chunks

{edges}

### Why this file looks the way it does — recent commits touching it

{why}

Each entry is a commit that changed this file: its `subject`, and `why` (the
opening of its message) when the author wrote one. This is the only record of
intent you have — the code itself cannot tell you why a choice was made. Use it
for the *why* half of a description, and only for claims the text actually
makes. Ignore entries that describe the edit without giving a reason.

Now return this file's feature set, inside solution tags.
