/** Pure re-export barrel — should produce a single __module__ chunk. */
export { Coordinator, makeOptions, sharedCoordinator } from "./sample_app";
export type { ClientOptions, QueryResult } from "./sample_app";
