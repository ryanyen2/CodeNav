def add(a: int, b: int) -> int:
    """Return the sum of two integers. Uses built-in +."""
    return a + b


def double(x: int) -> int:
    """Return twice the given integer."""
    return x * 2


def concat(a: str, b: str) -> str:
    """Concatenate two strings."""
    return a + b
